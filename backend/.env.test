DATABASE_URL="postgresql://postgres:12345@localhost:5432/chegg-scheduling-app-test?schema=public"
JWT_SECRET="test-jwt-secret-key-for-testing"
JWT_EXPIRES_IN_SECONDS="3600"
BOOTSTRAP_SECRET="test-bootstrap-secret-abc123"
ALLOW_SELF_REGISTER="false"
NODE_ENV="test"

# Weak-but-fast bcrypt in tests only (prod/dev default to 10). Keeps the many
# registration/hash calls from blocking the event loop, which reduces socket
# aborts under the suite's high request volume.
BCRYPT_SALT_ROUNDS="4"

# Raise rate-limit ceilings so repeated test calls don't hit 429
SENSITIVE_RATE_LIMIT_MAX="10000"
STANDARD_RATE_LIMIT_MAX="10000"
STRICT_RATE_LIMIT_MAX="10000"

# Test fixtures widely use meet.example.com/example.com as placeholder meeting
# links — allow them here only. Production defaults (zoom.us, zoomgov.com,
# skills.chegg.com) stay untouched; this does not weaken the real allow-list.
ALLOWED_MEETING_LINK_DOMAINS="zoom.us,zoomgov.com,skills.chegg.com,meet.example.com,example.com"
